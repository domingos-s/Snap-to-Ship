# Try Snap-to-Ship in 60 seconds

Open your repo in Codex.

Run:

/skills  
$snap-to-ship  

Upload any UI screenshot.

Choose:
- prototype mode (fast UI build)
- MVP mode (auth + database + CRUD)

Example prompts:
- Build this as a SaaS dashboard
- Turn this into a mobile responsive web app
- Create an MVP with login and database
- Improve UX but keep layout

Output:
A full Next.js + Tailwind + TypeScript app.
